.. _installation:

Installation
============

* To install django-reminders::

    pip install django-reminders

* Add ``'reminders'`` to your ``INSTALLED_APPS`` setting::

    INSTALLED_APPS = (
        # other apps
        "reminders",
    )
